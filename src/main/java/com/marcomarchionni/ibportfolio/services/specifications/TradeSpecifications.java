package com.marcomarchionni.ibportfolio.services.specifications;

import com.marcomarchionni.ibportfolio.models.Trade;
import org.springframework.data.jpa.domain.Specification;

import java.time.LocalDate;

public class TradeSpecifications {

//    public static Specification<Trade> tradeDateIsAfter(LocalDate tradeDateFrom) {
//        return (root, query, builder) -> builder.like(root.get("tradeDate"), contains(expression));
//    }
//
//    public static Specification<MarvelCharacter> lastNameContains(String expression) {
//        return (root, query, builder) -> builder.like(root.get("lastName"), contains(expression));
//    }
//
//    private static String contains(String expression) {
//        return MessageFormat.format("%{0}%", expression);
//    }
}
